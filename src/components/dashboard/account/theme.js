export const styles = theme => {
    // const spacing = theme.spacing.unit * 3;
    return {
        itemActions: {
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'flex-end'
        }
    }
}